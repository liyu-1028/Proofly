# 安全策略

## 支持版本

下表说明哪些版本会收到安全更新：

| 版本 | 是否支持 |
|------|---------|
| 0.1.x | ✅ |
| < 0.1 | ❌ |

## 报告漏洞

**请勿在公开 Issue 中披露安全漏洞。**

请通过以下方式私下报告：

- 📧 邮件：security@proofly.local（占位，正式发布时替换）
- 🔐 GitHub Private Vulnerability Reporting：仓库 → Security → Advisories → New draft security advisory

## 报告内容建议

请尽可能包含：

1. **漏洞类型**：XSS、SQL 注入、SSRF、权限绕过等
2. **影响范围**：哪些版本、哪些接口受影响
3. **复现步骤**：详细的 PoC
4. **潜在影响**：数据泄露、特权提升等
5. **已知缓解措施**：是否已有 workaround
6. **你的联系方式**：方便后续沟通

## 响应时间

| 阶段 | 预计时间 |
|------|---------|
| 初次确认 | 72 小时内 |
| 评估与复现 | 7 天内 |
| 修复与发布 | 14–30 天（视严重程度） |

## 严重程度分级

| 级别 | 定义 | 响应优先级 |
|------|------|-----------|
| 🔴 Critical | 远程代码执行、认证绕过、大规模数据泄露 | P0，24h 内 |
| 🟠 High | 权限提升、SQL 注入、敏感信息泄露 | P1，72h 内 |
| 🟡 Medium | XSS、CSRF、有限信息泄露 | P2，7 天内 |
| 🟢 Low | 信息泄露、理论风险 | P3，下个版本 |

## 安全最佳实践

### 部署侧

- ✅ **所有密钥通过环境变量注入**，不要硬编码
- ✅ 生产环境使用 **HTTPS**（TLS 终止在反向代理）
- ✅ 数据库密码使用 **强随机串**（≥ 32 字符）
- ✅ JWT secret 使用 **≥ 32 字节随机串**
- ✅ RSA 私钥通过 **专用密钥管理服务**（KMS / Vault）管理
- ✅ MinIO 启用 **服务端加密** 与访问日志
- ✅ 启用 Spring Security 的 **CSRF 保护**（如适用）
- ✅ 定期 **轮换密钥** 与数据库密码

### 开发侧

- ✅ 所有用户输入使用 `@Valid` 校验
- ✅ SQL 查询使用 MyBatis-Plus 参数化，**禁止**字符串拼接
- ✅ 密码使用 **BCrypt** 存储（cost ≥ 10）
- ✅ 审计关键操作到 `audit_log`
- ✅ 多租户隔离：所有业务表必须按 `store_id` 过滤
- ✅ 上传文件类型白名单 + 大小限制（默认 25MB）
- ✅ 审稿链接使用 **SHA-256 哈希**存储，数据库泄露也无法还原明文

## 已修复漏洞

暂无记录（首个公开发行版本）。

---

感谢你帮助保护 Proofly 与用户的安全！🛡️